spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Mescomptes
    username: root
    password: 1azerty.
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5Dialect
    open-in-view: true
    show_sql: false
